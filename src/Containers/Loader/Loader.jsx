import styles from './Loader.module.css';

const Loader = () => {

    return (
        <section>
            <span className={styles.loader}> </span>
        </section>
    );
};

export default Loader;